"use client";

// Google Tag Manager disabled for development speed

interface GoogleTagManagerProps {
  gtmId?: string;
}

export const GoogleTagManager = ({ gtmId }: GoogleTagManagerProps) => {
  // GTM disabled for dev performance
  return null;
};